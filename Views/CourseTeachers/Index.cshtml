@model IEnumerable<Asp_Labb2.Models.CourseTeacher>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Course)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentClass)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Course.CourseName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.TeacherName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentClass.StudentClassName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CourseTeacherId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CourseTeacherId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CourseTeacherId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
